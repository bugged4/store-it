{
  "name": "s3lambda",
  "version": "1.0.0",
  "description": "S3 Lambda function to write file metadata to MongoDB",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "deploy": "tsc && cd dist && npm install --omit=dev && zip -r function.zip . && aws lambda update-function-code --function-name YOUR_FUNCTION_NAME --zip-file fileb://function.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1015.0",
    "aws-lambda": "^1.0.7",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}